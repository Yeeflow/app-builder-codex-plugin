#!/usr/bin/env node
import assert from "node:assert/strict"; import { readFileSync } from "node:fs"; import { dirname, resolve } from "node:path"; import { fileURLToPath } from "node:url"; import { createSublistDescriptorHostContext, selectAndBindRaw, bindCompletedRecord, readForRules, readForCustomForm, selectionCount, disposeSublistDescriptorHostContext } from "./test-fixtures/data-list-sublist-frozen-descriptor-host-context-shadow.mjs";
const root=resolve(dirname(fileURLToPath(import.meta.url)),".."); const corpus=JSON.parse(readFileSync(resolve(root,"compatibility/differential-fixtures/data-list-sublist-frozen-descriptor-host-context-integration-shadow.v0.1.0.json"),"utf8")); const make=(c)=>Object.freeze({parentListId:c.parentListId,parentFieldId:c.parentFieldId,columns:Object.freeze(c.columns)});
const raw=Object.freeze({fieldName:"Text7"}); const record=Object.freeze({FieldID:corpus.cases[0].parentFieldId}); const descriptor=make(corpus.cases[0]); const first=createSublistDescriptorHostContext(); selectAndBindRaw(first,raw,descriptor); bindCompletedRecord(first,raw,record); assert.equal(readForRules(first,raw),descriptor); assert.equal(readForCustomForm(first,record),descriptor); assert.equal(selectionCount(first),1); assert.equal(JSON.stringify(first),"{}"); assert.equal(JSON.stringify(descriptor),JSON.stringify(make(corpus.cases[0])));
const overlapping=createSublistDescriptorHostContext(); assert.throws(()=>readForCustomForm(overlapping,record),/SUBLIST_COMPLETED_RECORD_NOT_BOUND/); selectAndBindRaw(overlapping,raw,descriptor); const record2=Object.freeze({FieldID:record.FieldID}); bindCompletedRecord(overlapping,raw,record2); assert.equal(readForCustomForm(overlapping,record2),descriptor); assert.throws(()=>selectAndBindRaw(first,raw,Object.freeze({...descriptor})),/SUBLIST_DESCRIPTOR_RECOMPUTATION_FORBIDDEN/); disposeSublistDescriptorHostContext(first); assert.throws(()=>readForRules(first,raw),/SUBLIST_DESCRIPTOR_CONTEXT_DISPOSED/); disposeSublistDescriptorHostContext(overlapping); assert.throws(()=>bindCompletedRecord(overlapping,raw,record2),/SUBLIST_DESCRIPTOR_CONTEXT_DISPOSED/);
console.log("SUBLIST_FROZEN_DESCRIPTOR_HOST_CONTEXT_INTEGRATION_SHADOW_IMPLEMENTED"); console.log("SUBLIST_FROZEN_DESCRIPTOR_SHARED_INSTANCE_PARITY_PASSED"); console.log("SUBLIST_FROZEN_DESCRIPTOR_SINGLE_SELECTION_DETERMINISM_PASSED"); console.log("SUBLIST_FROZEN_DESCRIPTOR_CONTEXT_ISOLATION_PASSED"); console.log("SUBLIST_FROZEN_DESCRIPTOR_CONTEXT_DISPOSAL_PASSED"); console.log("SUBLIST_FROZEN_DESCRIPTOR_CONTEXT_FAIL_CLOSED_GATES_PASSED"); console.log("SUBLIST_FROZEN_DESCRIPTOR_NON_SERIALIZED_CONTEXT_GUARD_PASSED"); console.log("SUBLIST_FROZEN_DESCRIPTOR_LEGACY_UNCHANGED");
