#!/usr/bin/env node
import { readFileSync } from "node:fs"; import { dirname, resolve } from "node:path"; import { fileURLToPath } from "node:url";
const root = resolve(dirname(fileURLToPath(import.meta.url)), ".."); const text = readFileSync(resolve(root, "scripts/lib/local-runtime-core-adapter.mjs"), "utf8"); const expected = ["lowerFixedFilterProjectionAtHost", "lowerDataListScalarResourceIdentityAtHost", "lowerDataListLookupResolutionAtHost", "lowerDataListType1IdentityControlPlacementAtHost", "lowerDataListSublistScalarSummaryIntentAtHost", "lowerDataListSublistDynamicSummaryIntentAtHost", "lowerDataListSublistNestedControlPlacementAtHost", "lowerDataListSublistEmbeddedLookupIntentAtHost", "lowerDataListSublistIdentityControlIntentAtHost"];
for (const code of ["LOCAL_RUNTIME_CORE_ADAPTER_ARTIFACT_MISSING", "LOCAL_RUNTIME_CORE_ADAPTER_ARTIFACT_LOAD_FAILED", "LOCAL_RUNTIME_CORE_ADAPTER_EXPORT_MISSING", "LOCAL_RUNTIME_CORE_ADAPTER_FORBIDDEN_RESOLUTION"]) assert(text.includes(code)); for (const name of expected) assert(text.includes(`export const ${name} = runtime.${name};`)); assert(!["packages/", "node_modules", "materialize-full-app-generated-final", "materializer-core-adapter"].some((x) => text.includes(x))); assert(JSON.stringify([...text.matchAll(/^export\s+(?:const|function|class)\s+(\w+)/gmu)].map((x) => x[1])) === JSON.stringify(expected)); console.log("LOCAL_RUNTIME_CORE_ADAPTER_VALID"); function assert(value) { if (!value) process.exit(1); }
