#!/usr/bin/env node
import assert from "node:assert/strict";
import { readFileSync } from "node:fs";
import { dirname, resolve } from "node:path";
import { fileURLToPath } from "node:url";
const root = resolve(dirname(fileURLToPath(import.meta.url)), ".."); const json = (path) => JSON.parse(readFileSync(resolve(root, path), "utf8")); const read = (path) => readFileSync(resolve(root, path), "utf8");
const closure = json("compatibility/capability-manifests/data-list-sublist-identity-control-configuration-family-closure.v0.1.0.json"); const fixture = json("compatibility/differential-fixtures/data-list-sublist-identity-control-export.v0.1.0.json"); const matrix = json("compatibility/capability-manifests/data-list-sublist-remaining-configuration-family-selection-matrix.v0.1.0.json"); const lineage = json("compatibility/capability-manifests/phase-closure-proof-lineage.v0.1.0.json"); const source = read("scripts/materialize-full-app-generated-final.mjs");
assert.equal(closure.decision.marker, "DATA_LIST_SUBLIST_IDENTITY_CONFIGURATION_FAMILY_CLOSURE_ACCEPTED"); assert.equal(fixture.selectedIdentityColumn.type, "user"); assert.equal(fixture.selectedIdentityColumn.control.type, "identity-picker"); assert.match(fixture.selectedIdentityColumn.parentListId, /^\d{19}$/); assert.match(fixture.selectedIdentityColumn.parentFieldId, /^\d{19}$/); assert.equal(matrix.families.find((x) => x.id === "barcode-control-and-field-rules").disposition, "product-unsupported"); assert.equal(matrix.families.find((x) => x.id === "nested-sublist-and-graph-mutation").disposition, "product-unsupported"); for (const token of ["DATA_LIST_SUBLIST_IDENTITY_CONFIGURATION_CORE_ROUTE_START", "routeDataListSublistIdentityControlAtHost", "DATA_LIST_SUBLIST_IDENTITY_CONFIGURATION_CORE_ROUTE_END"]) assert(source.includes(token)); assert(lineage.approvedTransitions.some((x) => x.phase === "phase-16d-data-list-sublist-identity-control-public-distribution-promotion")); assert(lineage.approvedTransitions.some((x) => x.phase === "phase-16e-data-list-sublist-identity-control-selective-routing-proof"));
console.log("DATA_LIST_SUBLIST_SPECIAL_CONTROL_EXPORT_AUDITED"); console.log("SUBLIST_IDENTITY_CONTROL_DUAL_DISTRIBUTION_VALID"); console.log("SUBLIST_IDENTITY_CONTROL_ROUTING_PASSED"); console.log("DATA_LIST_SUBLIST_IDENTITY_CONFIGURATION_FAMILY_CLOSURE_VALID"); console.log("DATA_LIST_SUBLIST_IDENTITY_CONFIGURATION_FAMILY_CLOSURE_REGRESSIONS_PASSED"); console.log("DATA_LIST_SUBLIST_IDENTITY_CONFIGURATION_FAMILY_CLOSURE_ACCEPTED");
