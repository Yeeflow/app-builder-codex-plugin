#!/usr/bin/env node
import assert from "node:assert/strict";
import { readFileSync } from "node:fs";
import { dirname, resolve } from "node:path";
import { fileURLToPath } from "node:url";
const root = resolve(dirname(fileURLToPath(import.meta.url)), ".."); const read = (path) => readFileSync(resolve(root, path), "utf8"); const json = (path) => JSON.parse(read(path)); const core = await import(`${new URL("../packages/app-builder-core-materializer/lib/index.js", import.meta.url).href}?retained=${Date.now()}`); const lineage = json("compatibility/capability-manifests/phase-closure-proof-lineage.v0.1.0.json");
assert.equal(typeof core.projectDashboardStaticConfiguration, "function"); const source = read("scripts/materialize-full-app-generated-final.mjs"); for (const token of ["buildDashboardKpiContracts", "buildDataAnalyticsRuntimeContract", "instantiateDashboardControlUuids", "replaceCollectionTemplatePlaceholders", "coreProjectDashboardStaticConfiguration"]) assert(source.includes(token), `CORE_EXTRACTION_WAVE3_BATCH_J_RETAINED_DASHBOARD_ROUTE:${token}`); for (const phase of ["core-extraction-wave3-batch-j-dashboard-static-configuration-selection-and-execution", "core-extraction-wave3-batch-i-data-list-static-configuration-selection-and-execution"]) assert(lineage.approvedTransitions.some((item) => item.phase === phase)); assert.equal(core.projectDashboardStaticConfiguration({ kind: "is-date-like-analytics-field", field: { fieldName: "DueDate" } }).isDateLike, true); console.log("CORE_EXTRACTION_WAVE3_BATCH_J_RETAINED_DASHBOARD_ROUTING_REGRESSIONS_PASSED");
