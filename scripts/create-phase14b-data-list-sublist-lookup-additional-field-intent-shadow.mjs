#!/usr/bin/env node

import { readFileSync, writeFileSync } from "node:fs";
import { dirname, resolve } from "node:path";
import { fileURLToPath } from "node:url";

const root = resolve(dirname(fileURLToPath(import.meta.url)), "..");
const contractPath = "compatibility/capability-manifests/data-list-sublist-lookup-additional-field-writeback-intent-shadow.v0.1.0.json";
const reportPath = "docs/architecture/yeeflow-app-builder-phase-14b-data-list-sublist-lookup-additional-field-writeback-intent-internal-shadow.v0.1.0.md";
const statePath = "docs/architecture/yeeflow-app-builder-core-migration-state.json";
const contract = { schemaVersion: "1.0.0", phase: "phase-14b-data-list-sublist-lookup-additional-field-writeback-intent-internal-shadow", decision: { status: "complete_static_shadow_only", marker: "SUBLIST_LOOKUP_ADDITIONAL_WRITEBACK_EXPORT_STATIC_PARITY_PASSED", nextPhase: "blocked-runtime-event-semantics-evidence-required" }, core: "Internal-only immutable static mapping intent; no host context, inventory, target record, event, expression, mutation, or package state is accepted.", host: "Test-only lowerer returns fresh addition metadata only and does not retrieve, bind, mutate, clear, or enforce readonly state.", runtimeParityClaimed: false, exclusions: ["selection change", "initialization", "edit mode", "repeat selection", "clear selection", "invalid target handling", "target retrieval", "destination mutation", "runtime read-only enforcement", "Approval Forms", "package output"] };
write(contractPath, contract);
write(reportPath, "# Phase 14B Embedded Sublist Lookup Additional-Field Static Intent Internal Shadow\n\nThe internal shadow reproduces only the export-proven static `Decimal5` to `LeaveUsageHours` addition metadata. It preserves direct target and parent scope strings, requires a readonly numeric destination in the same embedded Sublist mapping, returns frozen JSON-safe intent/findings, and lowers a fresh static addition record. No runtime writeback parity is claimed. Selection-change, initialization, edit-mode, repeat-selection, clear-selection, invalid-target, retrieval, evaluation, mutation, and read-only enforcement are unproven and excluded.\n");
const state = json(statePath); state.migration.currentPhase = contract.phase; state.migration.currentPhaseStatus = "complete"; state.migration.nextPhase = contract.decision.nextPhase; state.blocked ||= []; state.blocked.push({ id: "phase-14c-data-list-sublist-lookup-additional-field-runtime-distribution-and-routing", status: "blocked", reason: "The authoritative static export does not prove Data List Lookup selection-event timing, retrieval, clear-selection, invalid-target, destination mutation, or runtime read-only behavior. Public promotion or production metadata routing would imply unproven runtime semantics." }); state.completed.push({ id: contract.phase, status: "complete", evidence: "2026-07-19: internal-only static additional-field mapping shadow passed immutable export metadata parity; runtime writeback parity is explicitly not claimed." }); state.proofStatus.dataListSublistLookupAdditionalFieldWritebackIntentShadow = "passed_static_only"; state.proofStatus.dataListSublistLookupAdditionalFieldWritebackRuntime = "blocked_missing_runtime_event_evidence"; write(statePath, state); console.log("SUBLIST_LOOKUP_ADDITIONAL_WRITEBACK_STATIC_SHADOW_RECORDED");
function json(path) { return JSON.parse(readFileSync(resolve(root, path), "utf8")); }
function write(path, value) { writeFileSync(resolve(root, path), `${JSON.stringify(value, null, 2)}\n`); }
