#!/usr/bin/env node
import assert from "node:assert/strict";
import { cpSync, mkdtempSync, mkdirSync, readFileSync, rmSync, writeFileSync } from "node:fs";
import { execFileSync } from "node:child_process";
import { tmpdir } from "node:os";
import { dirname, resolve } from "node:path";
import { fileURLToPath, pathToFileURL } from "node:url";
const root=resolve(dirname(fileURLToPath(import.meta.url)),".."); const fixture=JSON.parse(readFileSync(resolve(root,"compatibility/differential-fixtures/core-extraction-wave3-batch-d-workflow-static-plan.v0.1.0.json"))); const temp=mkdtempSync(resolve(tmpdir(),"yeeflow-wave3d-"));
try { const source=await verify(root,"source"); const second=await verify(root,"source-second"); assert.deepEqual(source,second); verifyMaterializer(root,"source"); const rollback=createLegacyRollback(resolve(temp,"rollback")); verifyMaterializer(rollback,"rollback"); const zip=resolve(temp,"proof.zip"); execFileSync(process.execPath,[resolve(root,"scripts/build-plugin-archive.mjs"),"--output",zip],{cwd:root,stdio:"inherit"}); const archive=resolve(temp,"archive");mkdirSync(archive);execFileSync("unzip",["-q",zip,"-d",archive]);const archivePlugin=resolve(archive,"yeeflow-app-builder-plugin");assert.deepEqual(await verify(archivePlugin,"archive"),source); verifyMaterializer(archivePlugin,"archive"); const installed=resolve(temp,"installed/yeeflow-app-builder-plugin");cpSync(resolve(root,"dist/yeeflow-app-builder-plugin"),installed,{recursive:true});assert.deepEqual(await verify(installed,"installed"),source);verifyMaterializer(installed,"installed"); console.log("CORE_EXTRACTION_WAVE3_BATCH_D_SOURCE_PARITY_PASSED");console.log("CORE_EXTRACTION_WAVE3_BATCH_D_ARCHIVE_PARITY_PASSED");console.log("CORE_EXTRACTION_WAVE3_BATCH_D_INSTALLED_PARITY_PASSED");console.log("CORE_EXTRACTION_WAVE3_BATCH_D_MATERIALIZER_PARITY_PASSED");console.log("CORE_EXTRACTION_WAVE3_BATCH_D_LEGACY_ROLLBACK_PASSED");console.log("CORE_EXTRACTION_WAVE3_BATCH_D_DETERMINISM_IMMUTABILITY_LEAKAGE_PASSED"); } finally {rmSync(temp,{recursive:true,force:true});}
async function verify(surface,label){const path=surface===root?resolve(root,"packages/app-builder-core-planning/lib/index.js"):resolve(surface,"core/yeeflow-app-builder-core-planning.v0.1.0.mjs"); const core=await import(`${pathToFileURL(path).href}?${Date.now()}${Math.random()}`);assert.equal(typeof core.projectWorkflowStaticPlan,"function",`FACADE_MISSING:${label}`);assert.equal("workflowBusinessActionName" in core,false);const out=[];for(const c of fixture.cases){const input={kind:c.kind,value:structuredClone(c.value),options:structuredClone(c.options||{})};const before=structuredClone(input);const result=core.projectWorkflowStaticPlan(input);assert.deepEqual(result.value,c.expected,`${label}:${c.id}`);assert.deepEqual(input,before,`${label}:mutation:${c.id}`);assert(frozen(result));assert.deepEqual(JSON.parse(JSON.stringify(result)),result);out.push(result);}assert.throws(()=>core.projectWorkflowStaticPlan({kind:"execute-workflow",value:{}}),/Unsupported Workflow static-plan projection kind/);return out;}
function frozen(v){return !v||typeof v!=="object"||Object.isFrozen(v)&&Object.values(v).every(frozen)}
function verifyMaterializer(surface,label){const testPath=resolve(surface,"scripts/test-workflow-layout-golden-reference-gates.mjs");if(surface!==root)cpSync(resolve(root,"scripts/test-workflow-layout-golden-reference-gates.mjs"),testPath);execFileSync(process.execPath,[testPath],{cwd:surface,stdio:"ignore"});}
function createLegacyRollback(surface){cpSync(resolve(root,"scripts"),resolve(surface,"scripts"),{recursive:true});cpSync(resolve(root,"docs/reference"),resolve(surface,"docs/reference"),{recursive:true});cpSync(resolve(root,"dist/yeeflow-app-builder-plugin/core"),resolve(surface,"core"),{recursive:true});const materializer=resolve(surface,"scripts/materialize-full-app-generated-final.mjs");let text=readFileSync(materializer,"utf8");text=text.replace('import { projectWorkflowStaticPlan as coreProjectWorkflowStaticPlan } from "./lib/workflow-static-plan-core-adapter.mjs";','const coreProjectWorkflowStaticPlan = legacyWorkflowStaticPlan;');text+=`\nfunction legacyWorkflowStaticPlan(input){const kind=input.kind,v=input.value,o=input.options||{},clean=x=>String(x||\"\").trim(),norm=x=>clean(x).toLowerCase().replace(/[^a-z0-9]+/g,\" \").trim(),truncate=(x,n=48)=>{const t=clean(x).replace(/\\s+/g,\" \");return t.length<=n?t:(t.slice(0,n).replace(/\\s+\\S*$/,\"\").trim()||t.slice(0,n).trim())},normal=x=>{const t=clean(x),k=norm(t);if(!k)return \"\";if(/^start/.test(k))return \"StartNoneEvent\";if(/end\\s*reject|reject\\s*end/.test(k))return \"EndRejectEvent\";if(/^end/.test(k))return \"EndNoneEvent\";if(/sequence|flow|transition/.test(k))return \"SequenceFlow\";if(/inclusive|gateway|condition|branch|decision/.test(k))return \"InclusiveGateway\";if(/content\\s*list|master|create|update|archive|persist/.test(k)||t===\"ContentList\")return \"ContentList\";if(/candidate/.test(k))return \"CandidateTask\";return \"MultiAssignmentTask\"};let value; if(kind===\"is-default-action-name\")value=!clean(v)||/^(assignment\\s*task|candidate\\s*task|claim\\s*task|content\\s*list|inclusive\\s*gateway|gateway|task|workflow\\s*task|sequence\\s*flow(?:[_\\s-]*\\d+)?)$/i.test(clean(v));else if(kind===\"truncate-action-name\")value=truncate(v,Number(o.maxLength||48));else if(kind===\"normalize-node-type\")value=normal(v);else if(kind===\"layout-rows\"){const rows=[];for(const e of [...(Array.isArray(v)?v:[])].sort((a,b)=>a.position.y-b.position.y)){const r=rows.find(x=>Math.abs(x.y-e.position.y)<=o.tolerance);if(r){r.entries.push(e);r.y=r.entries.reduce((s,x)=>s+x.position.y,0)/r.entries.length}else rows.push({y:e.position.y,entries:[e]})}value=rows}else if(kind===\"rejected-vertices\"){const t=o.rejectPosition||{};if(!v||!t||Math.abs(t.x-v.x)<520&&Math.abs(t.y-v.y)<220)value=[];else{const x=v.x+Math.max(120,Math.round(Math.abs(t.x-v.x)/2));value=[{x,y:v.y},{x,y:t.y},{x:t.x-120,y:t.y}]}}else if(kind===\"content-list-operation\"){const t=[v?.nodeName,v?.description,v?.dataReadWrite].map(clean).join(\" \");value=/\\b(remove|delete)\\b/i.test(t)?\"remove\":/\\b(update|edit)\\b/i.test(t)?\"edit\":\"add\"}else if(kind===\"variable-id\")value=clean(v).toLowerCase().replace(/[^a-z0-9]+/g,\"_\").replace(/^_|_$/g,\"\")||\"workflow_variable\";else if(kind===\"variable-type\"){const k=norm(v);value=/user|identity/.test(k)?\"user\":/date|time/.test(k)?\"date\":/decimal|number|currency|percent|bigint/.test(k)?\"number\":/bit|boolean/.test(k)?\"boolean\":/file|image|upload/.test(k)?\"file\":\"text\"}else if(kind===\"infer-variable-assignee-name\"){const t=[v?.nodeName,v?.assigneeRole,v?.assignmentStrategy,v?.description].map(clean).join(\" \");const m=t.match(/\\b(?:workflow\\s+variables?|variable)\\s*[:=]?\\s*([A-Za-z][A-Za-z0-9_]*)\\b/i);value=m&&!/^line|department|manager|user$/i.test(m[1])?m[1]:/\\bowner\\b/i.test(t)?\"Owner\":\"\"}else if(kind===\"summarize-condition\")value=\"\";else if(kind===\"connector-description\")value=clean(v?.name)||\"Next\";else if(kind===\"query-result-field\")value={FieldID:String(v?.FieldID||v?.fieldId||v?.id||v?.fieldName||v?.FieldName||\"\"),FieldName:clean(v?.FieldName||v?.fieldName||v?.field||\"Title\"),DisplayName:clean(v?.DisplayName||v?.displayName||v?.name||v?.FieldName||\"Title\"),Type:clean(v?.Type||v?.type||v?.FieldType||v?.fieldType||\"Text\")};else throw Error(\"legacy projection kind\");return {kind,value}}\n`;writeFileSync(materializer,text);return surface;}
