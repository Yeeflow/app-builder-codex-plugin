#!/usr/bin/env node
import { readFileSync } from "node:fs"; import { dirname, resolve } from "node:path"; import { fileURLToPath } from "node:url"; const root=resolve(dirname(fileURLToPath(import.meta.url)),".."),json=p=>JSON.parse(readFileSync(resolve(root,p),"utf8"));const c=json("compatibility/capability-manifests/data-list-sublist-nested-control-family-closure.v0.1.0.json"),s=json("docs/architecture/yeeflow-app-builder-core-migration-state.json"),l=json("compatibility/capability-manifests/phase-closure-proof-lineage.v0.1.0.json"),routing=l.approvedTransitions.find(x=>x.phase==="phase-12e-data-list-sublist-nested-control-placement-selective-routing-proof");if(c.decision?.status!=="complete"||c.matrix?.length!==5||c.matrix.filter(x=>x.status==="routed").length!==1||c.matrix[0].family!=="scalar embedded child controls"||routing?.kind!=="routing"||s.proofStatus?.phase12Closure!=="accepted")throw Error("SUBLIST_NESTED_CONTROL_FAMILY_CLOSURE_INVALID");console.log("SUBLIST_NESTED_CONTROL_ROUTE_RECONFIRMED");console.log("SUBLIST_NESTED_CONTROL_FAMILY_CLOSURE_VALID");console.log("SUBLIST_NESTED_CONTROL_FAMILY_CLOSURE_REGRESSIONS_PASSED");console.log("PHASE_12_NESTED_CONTROL_FAMILY_CLOSURE_ACCEPTED");
