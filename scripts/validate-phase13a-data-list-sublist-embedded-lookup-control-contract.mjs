#!/usr/bin/env node
import { readFileSync } from "node:fs";import { dirname,resolve } from "node:path";import { fileURLToPath } from "node:url";const root=resolve(dirname(fileURLToPath(import.meta.url)),".."),json=p=>JSON.parse(readFileSync(resolve(root,p),"utf8"));const c=json("compatibility/capability-manifests/data-list-sublist-embedded-lookup-control-contract.v0.1.0.json"),m=json("compatibility/capability-manifests/data-list-sublist-embedded-lookup-control-ownership-matrix.v0.1.0.json"),s=readFileSync(resolve(root,"scripts/materialize-full-app-generated-final.mjs"),"utf8");if(c.decision?.status!=="blocked"||c.missingEvidence?.length!==4||!c.requiredCompositeScope?.includes("already resolved target ListID from Phase 6 host inventory")||m.rows?.length!==4||!s.includes("function parseSubListRowFields")||!s.includes("function shouldRouteDataListLookupResolution"))throw Error("SUBLIST_EMBEDDED_LOOKUP_SCOPE_CONTRACT_INVALID");console.log("SUBLIST_EMBEDDED_LOOKUP_SCOPE_CONTRACT_REGRESSIONS_PASSED");
