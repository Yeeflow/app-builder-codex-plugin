#!/usr/bin/env node
import { createHash } from "node:crypto";
import { readFileSync } from "node:fs";
import { dirname, resolve } from "node:path";
import { fileURLToPath } from "node:url";
const root = resolve(dirname(fileURLToPath(import.meta.url)), "..");
const read = (path) => readFileSync(resolve(root, path), "utf8");
const json = (path) => JSON.parse(read(path));
const fail = (code) => { console.error(code); process.exit(1); };
const state = json("docs/architecture/yeeflow-app-builder-core-migration-state.json");
const contract = json("compatibility/differential-fixtures/data-list-sublist-dynamic-summary-intent-shadow.v0.1.0.json");
const core = read("packages/app-builder-core-materializer/src/internal/data-list-sublist-dynamic-summary-intent.ts");
const host = read("scripts/test-fixtures/data-list-sublist-dynamic-summary-host-scope-context-shadow.mjs");
const source = read("scripts/materialize-full-app-generated-final.mjs");
const coreIndex = read("packages/app-builder-core-materializer/src/index.ts");
const adapter = read("scripts/lib/materializer-core-adapter.mjs");
const lineage = json("compatibility/capability-manifests/phase-closure-proof-lineage.v0.1.0.json");
const approvedDynamicSummaryRouting = lineage.approvedTransitions?.find((item) => item.phase === "phase-11f-sublist-summary-dynamic-intent-selective-routing-proof");
if (contract.phase !== "phase-11c-sublist-summary-scoped-dynamic-intent-internal-shadow" || contract.caseCount !== 16 || contract.sourceEvidence?.dataListExport?.sha256 !== "d36c2fa90a436e0d35d9d326f4790f459d8c884cda1b44bd52f3a272acb546aa") fail("SUBLIST_DYNAMIC_SUMMARY_CORPUS_INVALID");
for (const token of ["projectDataListSublistDynamicSummaryIntentInternal", "Object.freeze", "SUBLIST_DYNAMIC_SUMMARY_RUNTIME_EXPRESSION_FORBIDDEN", "SUBLIST_DYNAMIC_SUMMARY_HOST_CONTEXT_FORBIDDEN"]) if (!core.includes(token)) fail("SUBLIST_DYNAMIC_SUMMARY_CORE_BOUNDARY_INVALID");
if (/node:|codex|oauth|browser|git|openai|next|react|prisma/iu.test(core) || /tempVars|writeback|template|\bresource\b|package state/iu.test(core)) fail("SUBLIST_DYNAMIC_SUMMARY_CORE_BOUNDARY_INVALID");
for (const token of ["createDataListSublistDynamicSummaryHostScopeContext", "new WeakMap", "SUBLIST_DYNAMIC_SUMMARY_CONTEXT_DISPOSED", "SUBLIST_DYNAMIC_SUMMARY_APPROVAL_FORM_EXCLUDED"]) if (!host.includes(token)) fail("SUBLIST_DYNAMIC_SUMMARY_HOST_CONTEXT_INVALID");
if (/globalThis|module\.exports|\.push\(/u.test(host)) fail("SUBLIST_DYNAMIC_SUMMARY_HOST_CONTEXT_INVALID");
if (!approvedDynamicSummaryRouting && (adapter.includes("DynamicSummary") || /coreProjectDataListSublistDynamicSummary|DATA_LIST_SUBLIST_DYNAMIC_SUMMARY_(?:CORE_)?ROUTE/u.test(source))) fail("SUBLIST_DYNAMIC_SUMMARY_PREMATURE_ROUTE_OR_EXPORT");
const currentOrSuccessor = (state.migration?.currentPhase === contract.phase && state.migration?.currentPhaseStatus === "complete" && state.migration?.nextPhase === "phase-11d-sublist-summary-dynamic-intent-dual-public-distribution-readiness" && state.proofStatus?.phase11DynamicSummaryMigration === "shadow_passed_internal_only") || (state.migration?.currentPhase === "phase-11d-sublist-summary-dynamic-intent-dual-public-distribution-readiness" && state.migration?.currentPhaseStatus === "complete" && state.migration?.nextPhase === "phase-11e-sublist-summary-dynamic-intent-dual-public-distribution-promotion" && state.proofStatus?.dataListSublistDynamicSummaryIntentPublicReadiness === "accepted_not_promoted") || (state.migration?.currentPhase === "phase-11e-sublist-summary-dynamic-intent-dual-public-distribution-promotion" && state.migration?.currentPhaseStatus === "complete" && state.migration?.nextPhase === "phase-11f-sublist-summary-dynamic-intent-selective-routing-proof" && state.proofStatus?.dataListSublistDynamicSummaryIntentPublicDistribution === "passed") || (state.migration?.currentPhase === "phase-11g-sublist-summary-dynamic-family-closure-audit" && state.migration?.currentPhaseStatus === "complete" && state.migration?.overallStatus === "complete" && state.migration?.nextPhase === "" && state.proofStatus?.phase11Closure === "accepted");
if (!currentOrSuccessor) fail("SUBLIST_DYNAMIC_SUMMARY_STATE_INVALID");
if (!state.completed?.some((item) => item.id === contract.phase && item.status === "complete")) fail("SUBLIST_DYNAMIC_SUMMARY_STATE_INVALID");
if (!["a4fce6e0fa85ed578afd7e16f76b2558527605a9c193819eade455b3ed532b9d", approvedDynamicSummaryRouting?.sourceTransition?.afterSha256].includes(createHash("sha256").update(source).digest("hex"))) fail("SUBLIST_DYNAMIC_SUMMARY_LEGACY_CHANGED");
console.log("SUBLIST_DYNAMIC_SUMMARY_FROZEN_DESCRIPTOR_VALID");
