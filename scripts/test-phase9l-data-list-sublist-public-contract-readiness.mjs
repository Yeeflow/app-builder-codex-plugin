#!/usr/bin/env node
import assert from"node:assert/strict";import{readFileSync}from"node:fs";import{dirname,resolve}from"node:path";import{fileURLToPath}from"node:url";const r=resolve(dirname(fileURLToPath(import.meta.url)),".."),p=resolve(r,"compatibility/capability-manifests/data-list-sublist-embedded-schema-core-public-api-readiness.v0.1.0.json"),a=JSON.parse(readFileSync(p,"utf8"));for(const bad of["WeakMap","host context","template","resource","package","child resource ID","id/idx as identity"])assert.ok(a.excludes.includes(bad));assert.equal(a.publicExportAdded,false);assert.equal(a.artifactChanged,false);assert.equal(a.distributionChanged,false);console.log("SUBLIST_EMBEDDED_SCHEMA_PUBLIC_CONTRACT_AND_ROUTING_READINESS_REGRESSIONS_PASSED cases=10");
