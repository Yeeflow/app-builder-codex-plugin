#!/usr/bin/env node
import { createHash } from "node:crypto"; import { readFileSync, writeFileSync } from "node:fs"; import { dirname, resolve } from "node:path"; import { fileURLToPath } from "node:url";
const root=resolve(dirname(fileURLToPath(import.meta.url)),".."),lineagePath="compatibility/capability-manifests/phase-closure-proof-lineage.v0.1.0.json",closurePath="compatibility/capability-manifests/data-list-resource-definition-family-closure.v0.1.0.json",lineageText=readFileSync(resolve(root,lineagePath),"utf8"),lineage=JSON.parse(lineageText),closure=JSON.parse(readFileSync(resolve(root,closurePath),"utf8")); if(!lineage.approvedTransitions.some(x=>x.phase==="phase-11e-sublist-summary-dynamic-intent-dual-public-distribution-promotion"))throw Error("PHASE_11E_LINEAGE_MISSING"); closure.closureProofLineage.approvedPhases=lineage.approvedTransitions.map(x=>x.phase);closure.closureProofLineage.sha256=createHash("sha256").update(lineageText).digest("hex");writeFileSync(resolve(root,closurePath),`${JSON.stringify(closure,null,2)}\n`);console.log("PHASE_11E_CLOSURE_LINEAGE_MIRROR_RECONCILED");
