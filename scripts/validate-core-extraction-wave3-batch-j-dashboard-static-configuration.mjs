#!/usr/bin/env node
import assert from "node:assert/strict";
import { readFileSync } from "node:fs";
import { dirname, resolve } from "node:path";
import { fileURLToPath } from "node:url";
const root = resolve(dirname(fileURLToPath(import.meta.url)), ".."); const json = (path) => JSON.parse(readFileSync(resolve(root, path), "utf8")); const read = (path) => readFileSync(resolve(root, path), "utf8");
const phase = "core-extraction-wave3-batch-j-dashboard-static-configuration-selection-and-execution"; const contract = json(`compatibility/capability-manifests/${phase}.v0.1.0.json`); const registry = json("compatibility/capability-manifests/core-extraction-proof-envelope-registry.v1.4.0.json"); const plan = json("compatibility/capability-manifests/core-extraction-finite-wave-plan.v0.15.0.json"); const state = json("docs/architecture/yeeflow-app-builder-core-migration-state.json"); const source = read("scripts/materialize-full-app-generated-final.mjs"); const core = read("packages/app-builder-core-materializer/src/internal/dashboard-static-configuration.ts");
const selected = ["wave-3-envelope-80"]; const reclassified = ["wave-3-envelope-44", "wave-3-envelope-45", "wave-3-envelope-46", "wave-3-envelope-78", "wave-3-envelope-79", "wave-3-envelope-81", "wave-3-envelope-82", "wave-3-envelope-85", "wave-3-envelope-86"];
assert.equal(contract.marker, "CORE_EXTRACTION_WAVE3_BATCH_J_DASHBOARD_STATIC_CONFIGURATION_PASSED"); assert.deepEqual(contract.decision.extractedEnvelopes, selected); assert.deepEqual(contract.decision.reclassifiedHostOnlyEnvelopes, reclassified); for (const id of selected) assert.equal(registry.envelopes.find((item) => item.id === id)?.status, "accepted-extracted-and-routed"); for (const id of reclassified) assert.equal(registry.envelopes.find((item) => item.id === id)?.status, "reclassified-host-or-specialist-route");
for (const token of ["coreProjectDashboardStaticConfiguration", "normalize-dashboard-filters", "is-date-like-analytics-field"]) assert(source.includes(token)); for (const forbidden of ["node:fs", "node:path", "node:crypto", "WeakMap", "fetch(", "process."]) assert(!core.includes(forbidden));
assert.equal(contract.progress.wave3CoreExtractedEnvelopes, "28/89"); assert.equal(contract.progress.wave3TerminalDispositionEnvelopes.total, "74/89"); assert.equal(contract.progress.weightedCoreCompletion, 51.3371); assert.equal(plan.execution.nextPhase, "Wave 3 Batch K proof-envelope selection"); assert.equal(state.migration.currentPhase, phase); assert.equal(state.proofStatus.coreExtractionWave3BatchJDashboardStaticConfiguration, "passed_source_archive_installed_materializer_rollback"); console.log("CORE_EXTRACTION_WAVE3_BATCH_J_STRUCTURAL_VALID");
