#!/usr/bin/env node

import { createHash } from "node:crypto";
import { readFileSync } from "node:fs";
import { dirname, resolve } from "node:path";
import { fileURLToPath } from "node:url";

const root = resolve(dirname(fileURLToPath(import.meta.url)), "..");
const contract = json(argument("--contract", "compatibility/capability-manifests/data-list-sublist-summary-temp-variable-export-scope-evidence.v0.1.0.json"));
const matrix = json(argument("--matrix", "compatibility/capability-manifests/data-list-sublist-summary-temp-variable-export-scope-evidence-matrix.v0.1.0.json"));
const state = json("docs/architecture/yeeflow-app-builder-core-migration-state.json");
const lineage = json("compatibility/capability-manifests/phase-closure-proof-lineage.v0.1.0.json");
const source = read(argument("--source", "scripts/materialize-full-app-generated-final.mjs"));
const coreAdapter = read(argument("--core-adapter", "scripts/lib/materializer-core-adapter.mjs"));
const distribution = json("dist/yeeflow-app-builder-plugin/core/yeeflow-app-builder-core-distribution.v0.1.0.json");
const phase = "phase-11b-sublist-summary-runtime-temporary-variable-scope-evidence-audit";
const nextPhase = "phase-11c-sublist-summary-scoped-dynamic-intent-internal-shadow";
const approvedDynamicSummaryRouting = lineage.approvedTransitions?.find((item) => item.phase === "phase-11f-sublist-summary-dynamic-intent-selective-routing-proof");

if (contract.phase !== phase || contract.evidenceKind !== "read-only export-derived scope evidence" || contract.decision?.status !== "accepted_for_non_serialized_host_scope_context_only" || contract.decision?.marker !== "SUBLIST_SUMMARY_TEMP_VARIABLE_EXPORT_SCOPE_EVIDENCE_ACCEPTED" || contract.decision?.nextPhase !== nextPhase) fail("SUBLIST_SUMMARY_TEMP_VARIABLE_EXPORT_SCOPE_DECISION_INVALID");
if (JSON.stringify(contract.sources?.map((item) => item.fileName)) !== JSON.stringify(["Employee Leave Balances (1).ydl", "Leave Request.ywf"]) || contract.sources.some((item) => item.readOnly !== true || !/^[a-f0-9]{64}$/u.test(item.sha256))) fail("SUBLIST_SUMMARY_TEMP_VARIABLE_EXPORT_SOURCE_EVIDENCE_INVALID");
const dataList = contract.dataListScopeEvidence;
if (dataList?.parentList?.listIdKind !== "lossless-decimal-string" || dataList?.form?.layoutTitle !== "Create Balance" || dataList?.form?.layoutIdKind !== "lossless-decimal-string" || dataList?.form?.layoutResourceIdMatchesLayoutId !== true) fail("SUBLIST_SUMMARY_TEMP_VARIABLE_EXPORT_PARENT_SCOPE_INVALID");
for (const summary of [dataList?.listBoundSummary, dataList?.tempBoundSummary]) if (summary?.parentField?.type !== "list" || summary.parentField?.fieldIdKind !== "lossless-decimal-string" || summary.parentField?.sourceColumn?.id !== "Hours" || !summary.controlIdPresent || !summary.summaryIdPresent || !summary.sourceColumnMatchesListVariables) fail("SUBLIST_SUMMARY_TEMP_VARIABLE_EXPORT_SUMMARY_SCOPE_INVALID");
if (dataList?.listBoundSummary?.target?.bindingPrefix !== "__list_" || dataList.listBoundSummary.target?.parentFieldIdKind !== "lossless-decimal-string" || dataList?.tempBoundSummary?.target?.bindingPrefix !== "__temp_" || dataList.tempBoundSummary.target?.inventoryEntryMatchedExactlyOnce !== true || dataList.tempBoundSummary.target?.tempVariableIdxPresent !== true || dataList.summaryIdStandaloneIdentity !== false) fail("SUBLIST_SUMMARY_TEMP_VARIABLE_EXPORT_BINDING_RELATIONSHIP_INVALID");
if (!contract.safeHostContext?.requiredCompositeKey || JSON.stringify(contract.safeHostContext.requiredCompositeKey) !== JSON.stringify(["parentListId", "layoutId", "layoutResourceId", "parentFieldId", "sublistControlId", "summaryId"]) || !contract.safeHostContext.prohibitions?.includes("name-only matching without the composite scope") || !contract.safeHostContext.prohibitions?.includes("temporary-variable allocation") || !contract.safeHostContext.prohibitions?.includes("package serialization")) fail("SUBLIST_SUMMARY_TEMP_VARIABLE_HOST_CONTEXT_CONTRACT_INVALID");
if (contract.comparisonOnlyApprovalFormEvidence?.comparisonOnly !== true || contract.comparisonOnlyApprovalFormEvidence?.notReusableForDataList !== true || !contract.limitations?.some((item) => item.includes("runtime expression")) || !contract.limitations?.some((item) => item.includes("summary id is duplicated"))) fail("SUBLIST_SUMMARY_TEMP_VARIABLE_EXPORT_BOUNDARY_INVALID");
if (JSON.stringify(matrix.rows?.map((item) => item.bindingPrefix)) !== JSON.stringify(["__list_", "__temp_", "__variables_", "__temp_"]) || matrix.rows?.slice(0, 2).some((item) => item.eligibleForHostContext !== true) || matrix.rows?.slice(2).some((item) => item.comparisonOnly !== true || item.eligibleForDataListHostContext !== false) || !matrix.conclusion?.includes("sufficient for a bounded non-serialized Data List host scope context")) fail("SUBLIST_SUMMARY_TEMP_VARIABLE_EXPORT_MATRIX_INVALID");
if (!approvedDynamicSummaryRouting && /DATA_LIST_SUBLIST_DYNAMIC_SUMMARY_(?:CORE_)?ROUTE|coreProjectDataListSublistDynamicSummary|coreLowerDataListSublistDynamicSummary/iu.test(source)) fail("SUBLIST_SUMMARY_TEMP_VARIABLE_DYNAMIC_ROUTE_PREMATURE");
const currentArtifacts = Object.fromEntries(distribution.artifacts.map((item) => [item.packageName, { path: item.path, sha256: item.sha256 }]));
const approvedLaterArtifactPromotion = lineage.approvedTransitions?.find((item) => item.phase === "phase-11e-sublist-summary-dynamic-intent-dual-public-distribution-promotion")?.kind === "artifact-only" && JSON.stringify(lineage.approvedTransitions.find((item) => item.phase === "phase-11e-sublist-summary-dynamic-intent-dual-public-distribution-promotion")?.artifactState) === JSON.stringify(currentArtifacts);
const approvedLaterRouting = approvedDynamicSummaryRouting?.kind === "routing" && approvedDynamicSummaryRouting.sourceTransition?.afterSha256 === sha(source) && JSON.stringify(approvedDynamicSummaryRouting.artifactState) === JSON.stringify(currentArtifacts);
if (Object.values(contract.preserved || {}).some(Boolean) || ((contract.checksums?.protectedBoundaries?.materializer?.sha256 !== sha(source) || contract.checksums?.protectedBoundaries?.coreAdapter?.sha256 !== sha(coreAdapter)) && !approvedLaterRouting) || (!(JSON.stringify(contract.checksums?.artifacts) === JSON.stringify(currentArtifacts) || (approvedLaterArtifactPromotion && approvedLaterRouting))) || contract.checksums?.historicalZipSha256 !== "377dc6ddaf3b922c0543e3a77274e7a56f32b4e2f19452f4a73ed6ad7f8cd9e2") fail("SUBLIST_SUMMARY_TEMP_VARIABLE_EXPORT_AUDIT_MUTATION");
if (lineage.approvedTransitions.length < contract.lineage?.transitionCount || !contract.lineage.requiredPhases?.every((item) => lineage.approvedTransitions.some((entry) => entry.phase === item))) fail("PHASE_CLOSURE_PROOF_LINEAGE_INVALID");
const record = state.completed?.find((item) => item.id === phase);
const historicalBlock = state.blocked?.find((item) => item.id === "phase-11b-dynamic-summary-runtime-scope-evidence-missing");
const currentOrHistorical = (state.migration?.currentPhase === phase && state.migration?.currentPhaseStatus === "complete" && state.migration?.overallStatus === "in_progress" && state.migration?.nextPhase === nextPhase && state.proofStatus?.phase11DynamicSummaryMigration === "scope_evidence_accepted_internal_shadow_not_authorized") || (state.migration?.currentPhase === "phase-11c-sublist-summary-scoped-dynamic-intent-internal-shadow" && state.migration?.currentPhaseStatus === "complete" && state.migration?.overallStatus === "in_progress" && state.migration?.nextPhase === "phase-11d-sublist-summary-dynamic-intent-dual-public-distribution-readiness" && state.proofStatus?.phase11DynamicSummaryMigration === "shadow_passed_internal_only") || (state.migration?.currentPhase === "phase-11d-sublist-summary-dynamic-intent-dual-public-distribution-readiness" && state.migration?.currentPhaseStatus === "complete" && state.migration?.overallStatus === "in_progress" && state.migration?.nextPhase === "phase-11e-sublist-summary-dynamic-intent-dual-public-distribution-promotion" && state.proofStatus?.dataListSublistDynamicSummaryIntentPublicReadiness === "accepted_not_promoted") || (state.migration?.currentPhase === "phase-11e-sublist-summary-dynamic-intent-dual-public-distribution-promotion" && state.migration?.currentPhaseStatus === "complete" && state.migration?.overallStatus === "in_progress" && state.migration?.nextPhase === "phase-11f-sublist-summary-dynamic-intent-selective-routing-proof" && state.proofStatus?.dataListSublistDynamicSummaryIntentPublicDistribution === "passed") || (state.migration?.currentPhase === "phase-11g-sublist-summary-dynamic-family-closure-audit" && state.migration?.currentPhaseStatus === "complete" && state.migration?.overallStatus === "complete" && state.migration?.nextPhase === "" && state.proofStatus?.phase11Closure === "accepted");
if (!currentOrHistorical || record?.status !== "complete" || !record.evidence?.includes("SUBLIST_SUMMARY_TEMP_VARIABLE_EXPORT_SCOPE_EVIDENCE_ACCEPTED") || historicalBlock?.status !== "superseded_by_read_only_export_scope_evidence" || state.proofStatus?.dataListSublistSummaryTempVariableScopeEvidence !== "accepted_export_proven_nonserialized_host_context") fail("SUBLIST_SUMMARY_TEMP_VARIABLE_EXPORT_STATE_INVALID");
console.log("SUBLIST_SUMMARY_TEMP_VARIABLE_EXPORT_SCOPE_EVIDENCE_ACCEPTED");
console.log("SUBLIST_SUMMARY_TEMP_VARIABLE_HOST_CONTEXT_SCOPE_VALID");
console.log("SUBLIST_SUMMARY_TEMP_VARIABLE_RUNTIME_LIFECYCLE_STILL_DEFERRED");

function argument(option, fallback) { const index = process.argv.indexOf(option); return index < 0 ? fallback : process.argv[index + 1]; }
function read(path) { return readFileSync(resolve(root, path), "utf8"); }
function json(path) { return JSON.parse(read(path)); }
function sha(value) { return createHash("sha256").update(value).digest("hex"); }
function fail(code) { console.error(code); process.exit(1); }
