#!/usr/bin/env node
import assert from "node:assert/strict";
import { createHash } from "node:crypto";
import { readFileSync } from "node:fs";
import { dirname, resolve } from "node:path";
import { fileURLToPath } from "node:url";

const root = resolve(dirname(fileURLToPath(import.meta.url)), ".."); const read = (path) => readFileSync(resolve(root, path), "utf8"); const json = (path) => JSON.parse(read(path));
const phase = "core-extraction-wave3-batch-b-workflow-query-data-static-plan"; const contract = json("compatibility/capability-manifests/core-extraction-wave3-batch-b-workflow-query-data-static-plan.v0.1.0.json"); const inventory = json("compatibility/capability-manifests/core-extraction-production-ownership-inventory.v0.6.0.json"); const registry = json("compatibility/capability-manifests/core-extraction-proof-envelope-registry.v0.6.0.json"); const plan = json("compatibility/capability-manifests/core-extraction-finite-wave-plan.v0.7.0.json"); const graph = json("compatibility/capability-manifests/core-extraction-caller-graph.v0.2.0.json"); const distribution = json("dist/yeeflow-app-builder-plugin/core/yeeflow-app-builder-core-distribution.v0.1.0.json"); const planningApi = json("compatibility/capability-manifests/app-builder-core-planning-public-api.v0.1.0.json"); const state = json("docs/architecture/yeeflow-app-builder-core-migration-state.json"); const lineage = json("compatibility/capability-manifests/phase-closure-proof-lineage.v0.1.0.json");
assert.equal(contract.phase, phase); assert.equal(contract.marker, "CORE_EXTRACTION_WAVE3_BATCH_B_WORKFLOW_QUERY_DATA_STATIC_PLAN_EXECUTION_PASSED"); assert.equal(graph.marker, "CORE_EXTRACTION_WAVE3_BATCH_B_CALLER_GRAPH_PASSED"); assert.equal(contract.scope.selectedFunctionIds.length, 13); assert.equal(contract.scope.envelopeIds.length, 6);
for (const id of contract.scope.selectedFunctionIds) { const item = inventory.functions.find((entry) => entry.id === id); const assignment = registry.assignments.find((entry) => entry.functionId === id); assert.equal(item?.executionEvidence, "compatibility/capability-manifests/core-extraction-wave3-batch-b-workflow-query-data-static-plan.v0.1.0.json"); assert.equal(assignment?.executionEvidence, item.executionEvidence); assert.ok(item?.callerEvidence, `CORE_EXTRACTION_WAVE3_BATCH_B_CALLER_EVIDENCE_MISSING:${id}`); }
assert.equal(contract.callerInventory["scripts/lib/workflow-query-data-utils.mjs#buildWorkflowQueryDataProperties@25"], 1); assert.equal(contract.callerInventory["scripts/lib/workflow-query-data-utils.mjs#buildWorkflowListVariable@88"], 0); assert.ok(graph.edges.some((edge) => edge.kind === "imported-binding" && edge.toFunctionId.includes("#buildWorkflowQueryDataProperties@")), "CORE_EXTRACTION_WAVE3_BATCH_B_IMPORTED_CALLER_MISSING");
assert.equal(plan.waves.find((entry) => entry.id === "Wave 3")?.acceptedEnvelopeProgress, "2/89"); assert.equal(plan.execution.acceptedEnvelopeProgress, "2/89"); assert.equal(plan.execution.provisionalWeightedCoreCompletionScore, 30.5955); assert.equal(state.proofStatus.coreExtractionWave3AcceptedEnvelopeProgress, state.migration.currentPhase === "core-extraction-wave3-batch-d-workflow-static-plan" ? "6/89" : state.migration.currentPhase === "core-extraction-wave3-batch-c-data-list-sublist-static-configuration" ? "5/89" : "2/89");
assert.ok(planningApi.runtimeExports.includes("projectWorkflowQueryDataStaticPlan")); const planning = distribution.artifacts.find((entry) => entry.packageName === "@yeeflow/app-builder-core-planning"); assert.ok(planning?.exports.includes("projectWorkflowQueryDataStaticPlan")); const currentMaterializerSha = sha(read("scripts/materialize-full-app-generated-final.mjs")); if (currentMaterializerSha !== contract.materializer.afterSha256) { const successor = lineage.approvedTransitions.find((entry) => entry.phase === "core-extraction-wave3-batch-c-data-list-sublist-static-configuration"); assert.equal(successor?.sourceTransition?.beforeSha256, contract.materializer.afterSha256, "CORE_EXTRACTION_WAVE3_BATCH_B_SUCCESSOR_BASELINE_INVALID"); const latest = lineage.approvedTransitions.find((entry) => entry.phase === "core-extraction-wave3-batch-d-workflow-static-plan"); assert.equal(latest?.sourceTransition?.afterSha256, currentMaterializerSha, "CORE_EXTRACTION_WAVE3_BATCH_B_SUCCESSOR_SOURCE_INVALID"); } assert.equal(contract.materializer.beforeSha256, contract.materializer.afterSha256);
const shim = read("scripts/lib/workflow-query-data-utils.mjs"); assert.ok(shim.includes("projectWorkflowQueryDataStaticPlan")); assert.ok(shim.includes("export function parseWorkflowSorts")); assert.equal((shim.match(/return project\(/g) || []).length, 6, "CORE_EXTRACTION_WAVE3_BATCH_B_SHIM_ROUTE_COUNT_INVALID"); const core = read("packages/app-builder-core-planning/src/workflow-query-data-static-plan.ts"); assert.ok(!/node:|fetch\(|window\.|document\.|process\./iu.test(core), "CORE_EXTRACTION_WAVE3_BATCH_B_FORBIDDEN_CORE_BOUNDARY");
console.log("CORE_EXTRACTION_WAVE3_BATCH_B_EXECUTION_CONTRACT_VALID"); console.log("CORE_EXTRACTION_WAVE3_BATCH_B_CALLER_GRAPH_PASSED"); console.log("CORE_EXTRACTION_WAVE3_BATCH_B_PUBLIC_CONTRACT_PASSED");
function sha(value) { return createHash("sha256").update(value).digest("hex"); }
