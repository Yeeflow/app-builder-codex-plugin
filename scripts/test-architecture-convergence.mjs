#!/usr/bin/env node
import { mkdtempSync, readFileSync, rmSync, writeFileSync } from "node:fs";import { tmpdir } from "node:os";import { dirname, resolve } from "node:path";import { fileURLToPath } from "node:url";import { spawnSync } from "node:child_process";
const root=resolve(dirname(fileURLToPath(import.meta.url)),"..");const source=resolve(root,"compatibility/capability-manifests/yeeflow-app-builder-core-architecture-convergence.v0.1.0.json");const validator=resolve(root,"scripts/validate-architecture-convergence.mjs");const temp=mkdtempSync(resolve(tmpdir(),"yeeflow-architecture-convergence-"));try{expect(source,0,"ARCHITECTURE_CONVERGENCE_VALID");const c=JSON.parse(readFileSync(source,"utf8"));c.packages.pop();const p=resolve(temp,"missing.json");writeFileSync(p,JSON.stringify(c));expect(p,1,"ARCHITECTURE_CONVERGENCE_MAPPING_MISSING");console.log("ARCHITECTURE_CONVERGENCE_TESTS_PASSED cases=2")}finally{rmSync(temp,{recursive:true,force:true})}function expect(path,status,code){const r=spawnSync(process.execPath,[validator,"--contract",path],{cwd:root,encoding:"utf8"});if(r.status!==status||!`${r.stdout}${r.stderr}`.includes(code))throw new Error(`Expected ${code}`)}
