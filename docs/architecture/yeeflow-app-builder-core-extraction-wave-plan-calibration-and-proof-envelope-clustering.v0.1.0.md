# Core Extraction Wave Plan Calibration and Proof Envelope Clustering

`CORE_EXTRACTION_PROOF_ENVELOPE_CALIBRATION_ACCEPTED`

`CORE_EXTRACTION_PROOF_ENVELOPE_COVERAGE_VALID`

`CORE_EXTRACTION_PROOF_ENVELOPE_REGRESSIONS_PASSED`

## Decision

The prior per-function execution model is superseded for scheduling only. The ownership inventory remains the exact per-function authority. 451 remaining core-now functions are assigned once to 96 proof envelopes.

## Envelope Rule

Each envelope shares one source module, product surface, immutable input/output shape, and local dependency profile. The registry prohibits cross-surface grouping even when names are similar. Any Host/runtime mutation, Codex orchestration, browser/frontend behavior, external API behavior, or product enhancement requirement stops the envelope and requires reclassification.

## Waves

- Wave 1: 6 envelopes, 24 functions; Every member retains one surface, one signature, no mutable Host/runtime behavior, and a shared parity strategy.
- Wave 2: 1 envelopes, 1 functions; Every member retains one surface, one signature, no mutable Host/runtime behavior, and a shared parity strategy.
- Wave 3: 89 envelopes, 426 functions; Every member retains one surface, one signature, no mutable Host/runtime behavior, and a shared parity strategy.
- Core v1 closure: 0 envelopes, 0 functions; All Wave 1–3 envelopes are extracted, retained/reclassified with evidence, or explicitly moved to Core v2 by a new contract.

## Scores and Capacity

Baseline Core completion is 25/100. Wave 1: 27/100 if every envelope is accepted; Wave 2: 29/100 if every envelope is accepted; Wave 3: 100/100 if every envelope is accepted. Core v1 closure is 100/100 only after terminal disposition evidence, not merely implementation.

## Approval Form Lookup

The Phase 18B static Lookup configuration preservation remains one Wave 2 parity-ready candidate. This calibration does not extract it, expose a Core API, add an adapter, or change its production behavior.
