# Phase 14D Lookup Additional-Field Configuration Readiness Reconciliation

Phase 14C remains sealed historical evidence that the export does not prove frontend runtime execution. It is superseded only for materializer configuration generation. The authoritative Data List export proves one immutable declarative relationship: the `LeaveUsage` Lookup target field `Decimal5` maps to readonly embedded destination `LeaveUsageHours` through `attrs.addition[]`.

The approved route may project and emit only the existing static metadata shape. It does not retrieve a target record, react to selection, assign a value, clear a destination, evaluate an expression, enforce readonly at runtime, or mutate a template/resource at runtime. Those behaviors remain product frontend/runtime owned and out of scope.
