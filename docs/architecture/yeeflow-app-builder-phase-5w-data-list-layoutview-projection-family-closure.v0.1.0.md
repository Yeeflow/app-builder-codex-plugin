# Phase 5W Data List LayoutView Projection Family Closure

## Decision

`DATA_LIST_LAYOUTVIEW_PROJECTION_FAMILY_CLOSED`

Default and additional Data List Type 0 LayoutView structural projection are separately Core and Local Runtime proven. No remaining LayoutView construction path is a safely eligible immutable LayoutView projection vertical.

## Covered Paths

- Default Type 0: Materializer Core projects immutable fragment and filter intents; Local Runtime lowers allocated keys and findings; the host owns identity and integration.
- Additional Type 0: the same separation applies with an explicit immutable non-default intent and stable scope.

## Deferred Paths

- Type 1 custom forms require a template-graph contract.
- List display-layout references require host layout identity and resource mutation.
- Navigation and dashboard LayoutView paths are cross-surface host orchestration.

## Next Family

The next audit is `data-list-resource-record-identity-allocation-contract-audit`. Any future Core output must be an immutable resource-definition intent. The host owns identity maps, template loading and mutation, cross-resource lowering, package output, integration, and rollback.
