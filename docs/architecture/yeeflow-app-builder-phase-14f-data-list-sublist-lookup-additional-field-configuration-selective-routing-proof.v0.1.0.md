# Phase 14F Lookup Additional-Field Configuration Selective Routing Proof

The materializer now routes only declarative Data List embedded-Sublist Lookup `addition[]` configuration through one immutable Materializer Core projection. It emits the exact Legacy-shaped `{ FieldName, FieldID, IsShow, RelationName, Value: null, Order }` entry and static destination `readonly: true` metadata. Source, temporary official ZIP, and simulated installed Plugin outputs are deterministic and equal. The temporary-copy rollback removes only this configuration bridge and restores Legacy-shaped output.

No frontend or runtime behavior is simulated or claimed: selection events, target-record retrieval, automatic assignment, clear/edit handling, expression evaluation, readonly enforcement timing, and runtime writeback remain Yeeflow product frontend/runtime responsibilities.
