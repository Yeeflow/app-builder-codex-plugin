"# Phase 13D Embedded Sublist Lookup Dual Public-Distribution Readiness\n\nBoth prospective APIs are accepted for coordinated official distribution. Core exposes only immutable target/display intent. Local Runtime returns only new static Lookup list-field metadata from explicit host bindings. Target inventory resolution remains owned by the existing Phase 6 host contract. `addition[]`, cross-field transfer, and runtime read-only enforcement are excluded.\n"
