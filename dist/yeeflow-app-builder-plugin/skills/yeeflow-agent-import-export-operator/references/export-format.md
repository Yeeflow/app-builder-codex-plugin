The 2026-08-04 standalone Yeeflow `.yaia` export baseline uses exactly `Category`, `Description`, `IconUrl`, `Name`, `PackageJson`, `TemplateId`, and `Type`. `Type` is `0`; the observed `Category` and `TemplateId` values are `null`; `IconUrl` is a string. Preserve `PackageJson` as an opaque canonical Base64 payload unless the official import/export path returns a normalized `importRead` result. Do not downgrade the profile to the earlier four-field inference.
